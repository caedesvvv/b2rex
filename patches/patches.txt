ogredotscene.patch

adds global location offset to scene export.

modrex_b2rex0.2.patch:
  * patch for modrex with needed features for the server

ogrepkg.patch:
changes gameengine mode exporter materials:
  * add normal and specular shaders
  * fix tripping on unexisting materials
  * ignore vertexColors as they malform the mesh for the ogre importer on taiga

patch01.patch:
  * patch for opensim to fix xmlrpc connections getting stuck.


